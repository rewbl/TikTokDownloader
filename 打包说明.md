# TikTok Downloader 打包说明

## 🎉 打包完成

已成功将 `main_notion_monitor.py` 打包成独立的可执行文件。

## 📁 文件说明

- `build.py` - 一键打包脚本
- `dist/notion_monitor.exe` - 打包后的可执行文件 (57.0 MB)

## 🚀 使用方法

### 运行程序
```bash
# 直接运行
dist/notion_monitor.exe

# 或者双击 dist/notion_monitor.exe
```

### 重新打包
```bash
# 当代码更新后，重新打包
python build.py
```

## ✅ 系统要求

### 运行环境
- Windows 10/11 (64-bit)
- 网络连接
- 正确的 Notion 和 Slack 配置

### 开发环境（重新打包时）
- Python 3.11+
- Poetry
- 项目依赖已安装 (`poetry install`)

## 🔧 故障排除

### 运行时问题
1. **程序无法启动**
   - 检查是否有杀毒软件阻止
   - 确保网络连接正常

2. **配置错误**
   - 检查 Notion API 配置
   - 检查 Slack Bot Token

### 重新打包问题
1. **环境检查失败**
   ```bash
   poetry install  # 安装依赖
   ```

2. **构建失败**
   - 确保在项目根目录运行
   - 检查 Poetry 环境是否正常

## 📞 支持

如果遇到问题：
1. 检查程序生成的日志文件
2. 确保所有配置正确
3. 重新运行 `python build.py` 打包

---
**打包时间**: 2025-07-29  
**文件大小**: 57.0 MB  
**状态**: ✅ 可用
